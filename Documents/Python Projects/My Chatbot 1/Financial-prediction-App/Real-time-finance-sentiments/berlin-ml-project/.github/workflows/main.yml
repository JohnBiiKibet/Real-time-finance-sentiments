name: ML Pipeline CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build Docker Image
        run: docker build -t finance-ml-app .
      - name: Run Unit Tests
        run: pip install -r requirements.txt pytest && pytest test_app.py
